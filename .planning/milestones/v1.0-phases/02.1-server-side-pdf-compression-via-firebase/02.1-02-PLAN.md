---
phase: 02.1-server-side-pdf-compression-via-firebase
plan: 02
type: execute
wave: 2
depends_on:
  - "01-PLAN"
files_modified:
  - index.html
autonomous: false
requirements:
  - SCOMP-03
  - SCOMP-04
  - SCOMP-06
user_setup: []

must_haves:
  truths:
    - "User can select Server (Ghostscript) mode and see three preset options (Best Quality, Balanced, Compressed) with descriptions including DPI values"
    - "When Server mode is selected, a privacy disclosure is visible stating files are temporarily sent to the server for compression"
    - "The old Heavy mode JPEG re-render warning about text layer destruction is removed — server mode preserves text"
    - "Clicking Compress PDFs in server mode sends each file to the Firebase Cloud Function and displays before/after results"
    - "Fast (Lossless) client-side mode continues to work exactly as before"
    - "Server mode errors (network, server failure) are shown per-file in the results list without stopping the batch"
    - "Server mode has a lower per-file size gate (32 MB) than Fast mode (80 MB) due to Cloud Function request limits"
  artifacts:
    - path: "index.html"
      provides: "Updated compress section with server mode, privacy disclosure, compressFileServer(), updated compressAllFiles()"
      contains: "COMPRESS_PDF_ENDPOINT, compressFileServer, Server (Ghostscript), privacy-disclosure, 32 MB"
  key_links:
    - from: "compressAllFiles()"
      to: "compressFileServer(file, preset)"
      via: "conditional on compressMode === 'server'"
      pattern: "compressMode.*server.*compressFileServer"
    - from: "compressFileServer()"
      to: "COMPRESS_PDF_ENDPOINT"
      via: "fetch with FormData (multipart POST)"
      pattern: "fetch.*COMPRESS_PDF_ENDPOINT.*FormData"
    - from: "mode toggle radio"
      to: "privacy disclosure visibility"
      via: "change event listener"
      pattern: "compressMode.*server.*privacy"
---

<objective>
Update the Compress PDF frontend in `index.html` to replace the client-side Heavy (JPEG Re-render) mode with a Server (Ghostscript) mode that calls the deployed Firebase Cloud Function. Add a privacy disclosure for server mode, update preset descriptions with DPI info, and wire `compressFileServer()` into the existing compression orchestrator.

Purpose: The old Heavy mode destroyed text selectability by rasterizing pages to JPEG. Server mode sends PDFs to Ghostscript which downsamples only raster images while preserving text vectors and fonts. The frontend change is purely a swap of the compression path — results display, download buttons, and progress all work identically.

Output: Updated `index.html` with server mode UI, `compressFileServer()` function, privacy disclosure, and updated `compressAllFiles()` orchestrator.
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/franc/dev/pdf_merger/.planning/ROADMAP.md
@C:/Users/franc/dev/pdf_merger/.planning/STATE.md
@C:/Users/franc/dev/pdf_merger/.planning/phases/02.1-server-side-pdf-compression-via-firebase/02.1-RESEARCH.md
@C:/Users/franc/dev/pdf_merger/.planning/phases/02.1-server-side-pdf-compression-via-firebase/02.1-server-side-pdf-compression-via-firebase-01-SUMMARY.md

<interfaces>
<!-- Existing Compress PDF state variables (from Phase 02 Plan 01): -->
<!-- let compressFiles = [];         // Array of File objects -->
<!-- let compressMode = 'fast';      // Currently 'fast' | 'heavy' — will become 'fast' | 'server' -->
<!-- let compressResults = [];       // [{fileName, originalSize, compressedBytes}] -->
<!-- let compressPreset = 'balanced'; // 'best' | 'balanced' | 'compressed' -->
<!-- const compressPresetValues = { best: 0.92, balanced: 0.75, compressed: 0.55 }; — WILL BE REMOVED -->

<!-- Existing DOM references: -->
<!-- uploadAreaCompress, fileInputCompress, fileListCompress -->
<!-- compressActions, clearBtnCompress, compressBtnCompress -->
<!-- compressHeavyWarning — WILL BE REPLACED with privacy disclosure -->
<!-- compressQualityGroup — visibility logic changes (show for server mode) -->
<!-- compressResultsList -->

<!-- Existing functions that remain unchanged: -->
<!-- addCompressFiles(files), removeCompressFile(index), renderCompressFileList() -->
<!-- updateCompressButtons(), clearCompressTool(), formatBytes(bytes) -->
<!-- renderCompressResult(fileName, originalSize, afterSize, resultIndex) -->
<!-- renderCompressError(fileName, errorMsg), downloadCompressedPdf(resultIndex) -->
<!-- compressFast(arrayBuffer) — Fast mode stays exactly as-is -->

<!-- Functions to be REMOVED: -->
<!-- compressFileHeavy(arrayBuffer, quality, onPageProgress) — replaced by compressFileServer() -->
<!-- dataUrlToUint8Array(dataUrl) — only used by compressFileHeavy, no longer needed -->

<!-- Functions to be ADDED: -->
<!-- compressFileServer(file, preset) — sends File to Cloud Function, returns Uint8Array -->

<!-- Shared utilities (unchanged): -->
<!-- showStatus(message, type, statusId), hideStatus(statusId) -->
<!-- updateProgress(percent, barId, fillId), hideProgress(barId, fillId) -->
<!-- escapeHtml(text), isEncryptedPdf(arrayBuffer) -->

<!-- Cloud Function endpoint URL: -->
<!-- Provided by user in Plan 01 Task 2 checkpoint (pasted as resume signal) -->
<!-- Stored as: const COMPRESS_PDF_ENDPOINT = 'https://us-central1-XXX.cloudfunctions.net/compressPdf'; -->

<!-- CRITICAL: Do NOT set Content-Type header in fetch() when using FormData -->
<!-- The browser must set it automatically with the multipart boundary -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Compress PDF UI and wire server-side compression</name>
  <files>index.html</files>
  <action>
Make the following changes to `index.html`. All changes are within the Compress PDF section (HTML and JS blocks).

**HTML Changes (in the compress-pdf section):**

1. **Replace "Heavy (JPEG Re-render)" radio option** with "Server (Ghostscript)" mode:
   - Change the radio `value="heavy"` to `value="server"`
   - Change the option title from "Heavy (JPEG Re-render)" to "Server (Ghostscript)"
   - Change the description from "Re-renders each page as a JPEG image for maximum size reduction. Text will no longer be selectable." to "Server-side compression via Ghostscript. Preserves text selectability while downsampling images. Requires internet connection."

2. **Replace the Heavy mode warning** (`id="compressHeavyWarning"`) with a privacy disclosure:
   - Change `id="compressHeavyWarning"` to `id="compressPrivacyNotice"`
   - Change CSS class from `compress-heavy-warning` to `compress-privacy-notice`
   - Change content from the text layer destruction warning to: `<strong>Privacy Notice:</strong> Server mode sends your PDF to our secure server for compression. Files are processed immediately and deleted — they are never stored. For fully private processing, use Fast (Lossless) mode instead.`
   - Keep `style="display:none;"` — shown only when server mode is selected

3. **Update preset descriptions** in the `compressQualityGroup` to include DPI values:
   - Best Quality: "Minimal compression — highest visual fidelity (300 DPI)"
   - Balanced: "Good balance of quality and file size (150 DPI)"
   - Compressed: "Maximum size reduction — still readable (72 DPI)"

4. **Update the upload hint** — the upload area hint says "max 80 MB each". This needs to dynamically reflect the mode, or simply state the lower limit. Change the static text to: "Upload one or more PDF files (max 80 MB each)"
   Leave as-is — the JS-level gate will enforce the per-mode limit. The existing 80 MB text is the maximum possible.

5. **Update the privacy badge** — the existing privacy badge says "100% Private - All processing happens in your browser. No files are uploaded to any server." This is no longer unconditionally true. Change it to:
   `🔒 <strong>Privacy First</strong> - Fast mode processes entirely in your browser. Server mode sends files to our secure server for processing.`

**JavaScript Changes (in the COMPRESS PDF SECTION JS block):**

6. **Add endpoint constant** near the top of the Compress PDF JS section, right after the `// ========== COMPRESS PDF SECTION ==========` comment:
   ```javascript
   const COMPRESS_PDF_ENDPOINT = 'PASTE_YOUR_ENDPOINT_URL_HERE';
   ```
   The executor MUST read the Plan 01 SUMMARY.md to find the actual deployed endpoint URL provided by the user. Replace the placeholder with the real URL.

7. **Update compressMode initial value comment** — change `// 'fast' | 'heavy'` to `// 'fast' | 'server'`

8. **Remove `compressPresetValues` object** — the line `const compressPresetValues = { best: 0.92, balanced: 0.75, compressed: 0.55 };` is no longer needed (presets are sent as strings to the server). DELETE this line entirely.

9. **Update the mode toggle event listener:**
   - Change `compressMode === 'heavy'` checks to `compressMode === 'server'`
   - Change the variable reference from `compressHeavyWarning` to `compressPrivacyNotice` (matching the renamed DOM element)
   - Update the DOM reference line from `const compressHeavyWarning = document.getElementById('compressHeavyWarning');` to `const compressPrivacyNotice = document.getElementById('compressPrivacyNotice');`

10. **Update `clearCompressTool()`:**
    - Change `compressMode = 'fast';` stays the same
    - Change `compressHeavyWarning.style.display = 'none';` to `compressPrivacyNotice.style.display = 'none';`
    - Keep all other reset logic the same

11. **Remove `dataUrlToUint8Array()` function** — it was only used by `compressFileHeavy()` which is being removed.

12. **Remove `compressFileHeavy()` function** — the entire function from `async function compressFileHeavy(...)` to its closing `}` must be deleted.

13. **Add `compressFileServer()` function** in place of `compressFileHeavy()`:
    ```javascript
    async function compressFileServer(file, preset) {
        const formData = new FormData();
        formData.append('pdf', file);
        formData.append('preset', preset);

        const response = await fetch(COMPRESS_PDF_ENDPOINT, {
            method: 'POST',
            body: formData
            // CRITICAL: Do NOT set Content-Type header — browser must set boundary automatically
        });

        if (!response.ok) {
            let errorMsg = 'Server error: ' + response.status;
            try {
                const errBody = await response.json();
                errorMsg = errBody.error || errorMsg;
            } catch (_) {}
            throw new Error(errorMsg);
        }

        const arrayBuffer = await response.arrayBuffer();
        return new Uint8Array(arrayBuffer);
    }
    ```

14. **Update `compressAllFiles()` orchestrator:**
    - In the `if (compressMode === 'fast')` / `else` block:
      - The `if (compressMode === 'fast')` branch stays exactly as-is
      - Replace the `else` branch (which currently calls `compressFileHeavy`) with:
        ```javascript
        } else {
            // Server mode: send to Firebase Cloud Function
            showStatus('<div class="spinner"></div> Compressing (Server): ' + escapeHtml(file.name) + '...', 'loading', 'statusCompress');
            compressedBytes = await compressFileServer(file, compressPreset);
        }
        ```
      - Remove the `const quality = parseInt(compressQualitySlider.value, 10) / 100;` line from the else branch (there is no slider anymore)
      - Remove the per-page progress callback from the else branch (server mode has no page-level progress — it's a single request/response)

15. **Add server-mode file size gate** in `addCompressFiles()`:
    The existing 80 MB gate applies to all modes. For server mode specifically, add a check: if `compressMode === 'server'` and `file.size > 32 * 1024 * 1024`, skip the file with a message stating the server mode limit is 32 MB.

    Actually, since files are added BEFORE compression and the mode can change between adding and compressing, the size gate should be in `compressAllFiles()` instead — check each file's size against the server limit at compression time, not at upload time. Add this check in the `else` (server mode) branch of `compressAllFiles()`, before calling `compressFileServer()`:
    ```javascript
    if (file.size > 32 * 1024 * 1024) {
        renderCompressError(file.name, 'File exceeds 32 MB server mode limit. Use Fast mode for large files.');
        processedCount++;
        updateProgress((processedCount / totalFiles) * 100, 'progressBarCompress', 'progressFillCompress');
        continue;
    }
    ```

16. **Update `clearCompressTool()`** to reset `compressMode` radio to `fast` — this already happens. Also ensure the privacy notice is hidden. Replace `compressHeavyWarning.style.display = 'none';` with `compressPrivacyNotice.style.display = 'none';`.

CRITICAL ANTI-PATTERNS:
- Do NOT set Content-Type header in the fetch call for FormData — the browser MUST set it with the boundary
- Do NOT remove the Fast mode code path — it stays client-side and unchanged
- Do NOT remove `compressFast()` — only `compressFileHeavy()` and `dataUrlToUint8Array()` are removed
- Do NOT change any other tool section (PDF Tools, Watermark, Split PDF)
  </action>
  <verify>
    <automated>cd C:/Users/franc/dev/pdf_merger && node -e "const fs = require('fs'); const html = fs.readFileSync('index.html','utf8'); const checks = [ html.includes('COMPRESS_PDF_ENDPOINT'), html.includes('compressFileServer'), html.includes('compressPrivacyNotice'), !html.includes('compressFileHeavy'), !html.includes('dataUrlToUint8Array'), html.includes('value=\"server\"'), !html.includes('value=\"heavy\"'), html.includes('300 DPI'), html.includes('150 DPI'), html.includes('72 DPI'), html.includes('32 MB server mode limit') ]; console.log('Checks:', checks); if (checks.every(c=>c)) console.log('ALL PASSED'); else { checks.forEach((c,i) => { if(!c) console.log('FAILED check', i) }); process.exit(1); }"</automated>
  </verify>
  <done>index.html updated: Heavy mode replaced with Server (Ghostscript) mode. Privacy disclosure visible when server mode selected. Preset descriptions include DPI values. compressFileServer() sends PDF to Cloud Function via fetch/FormData. compressAllFiles() routes to server function when mode is 'server'. 32 MB server-mode size gate enforced at compression time. compressFileHeavy() and dataUrlToUint8Array() removed. Fast mode unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Server (Ghostscript) compression end-to-end</name>
  <action>Human verification of complete Server (Ghostscript) compression feature end-to-end.</action>
  <what-built>Replaced client-side Heavy (JPEG Re-render) mode with Server (Ghostscript) mode that sends PDFs to Firebase Cloud Function for Ghostscript compression. Added privacy disclosure, updated preset descriptions with DPI values, and enforced 32 MB server-mode file size limit. Fast (Lossless) mode unchanged.</what-built>
  <how-to-verify>
Test the following flows in order (open index.html in a browser):

**Flow 1 — Fast Mode Still Works (regression check):**
1. Click "Compress PDF" in the nav
2. Verify "Fast (Lossless)" mode is selected by default
3. Upload any unencrypted PDF
4. Click "Compress PDFs" — should complete with before/after sizes in results
5. Download the compressed file — it should be a valid PDF
6. Verify NO privacy notice is shown in Fast mode

**Flow 2 — Server Mode Compression:**
1. Click "Clear All" to reset
2. Select "Server (Ghostscript)" mode
3. Verify the privacy disclosure appears: "Privacy Notice: Server mode sends your PDF to our secure server..."
4. Verify preset options are visible with DPI values: "Best Quality (300 DPI)", "Balanced (150 DPI)", "Compressed (72 DPI)"
5. Select "Balanced" preset (should be default)
6. Upload a PDF with text and images (2-5 pages)
7. Click "Compress PDFs" — status should show "Compressing (Server)..."
8. After completion, verify:
   - Before/after sizes displayed
   - Download button present
9. Download the compressed PDF
10. Open the downloaded PDF — **TEXT MUST BE SELECTABLE** (this is the key improvement over old Heavy mode)

**Flow 3 — Server Mode Presets:**
1. Clear, select Server mode, upload same PDF
2. Compress with "Best Quality" preset — note the output size
3. Clear, upload same PDF, compress with "Compressed" preset — output should be noticeably smaller than Best Quality
4. Both outputs should have selectable text

**Flow 4 — Server Mode Size Gate:**
1. If you have a PDF larger than 32 MB: upload it in Server mode
2. Click Compress — the large file should show error "File exceeds 32 MB server mode limit. Use Fast mode for large files."
3. Switch to Fast mode with the same file — it should compress normally (Fast allows up to 80 MB)

**Flow 5 — Mode Toggle Behavior:**
1. Select Server mode — privacy notice and presets appear
2. Select Fast mode — privacy notice and presets disappear
3. Select Server mode again — both reappear
4. Click "Clear All" — mode resets to Fast, privacy notice hidden

**Flow 6 — Error Handling:**
1. (Optional) Disconnect internet, try Server mode — should show a network error in results
2. Upload an encrypted PDF in Server mode — should show encryption error (client-side check, same as before)

**Flow 7 — No Old Heavy Mode Remnants:**
1. Confirm there is NO "Heavy (JPEG Re-render)" option anywhere in the UI
2. Confirm there is NO "text will not be selectable" warning
3. Confirm DevTools console shows no JavaScript errors during any flow

**Flow 8 — Other Tools Regression:**
1. Click "PDF Tools" — works normally
2. Click "Split PDF" — works normally
3. Click "Watermark Inserter" — works normally

**Expected result for approval:** All flows pass. Server mode produces compressed PDFs with selectable text. Fast mode unchanged. No JS errors.
  </how-to-verify>
  <resume-signal>Type "approved" if all flows pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- Server mode selected → privacy disclosure visible, presets shown with DPI values
- Fast mode selected → privacy disclosure hidden, presets hidden
- Server mode compression → PDF sent to Cloud Function, compressed PDF returned, before/after displayed
- Text is selectable in server-compressed PDFs
- Fast mode compression → same as before (client-side pdf-lib, no network)
- 32 MB server mode size gate prevents oversized files
- Old Heavy mode code completely removed (no compressFileHeavy, no dataUrlToUint8Array, no JPEG quality references)
- No JavaScript errors during any flow
- No regressions in other tools (PDF Tools, Split PDF, Watermark)
</verification>

<success_criteria>
- "Server (Ghostscript)" replaces "Heavy (JPEG Re-render)" in mode selection
- Privacy disclosure visible only in server mode
- Preset descriptions show DPI values (300, 150, 72)
- Server mode produces compressed PDFs with selectable text
- Fast mode is completely unchanged and functional
- compressFileHeavy() and dataUrlToUint8Array() removed from codebase
- compressFileServer() correctly sends FormData to COMPRESS_PDF_ENDPOINT
- 32 MB per-file gate enforced for server mode at compression time
- All existing tools continue working without regressions
- Zero JavaScript errors in DevTools console
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-server-side-pdf-compression-via-firebase/02.1-server-side-pdf-compression-via-firebase-02-SUMMARY.md`
</output>
