---
phase: 03-split-merge-pdfs-and-pdf-to-images-into-separate-nav-tabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [NAV-01, NAV-03]

must_haves:
  truths:
    - "Merge PDFs has its own nav tab button and clicking it shows only the Merge PDFs section"
    - "Merge PDFs section has independent state — mergeFiles array, mergeDraggedIndex, and all merge-specific DOM refs"
    - "User can upload multiple PDFs via click or drag-and-drop in the Merge section and see a reorderable file list"
    - "User can click Merge PDFs button and get a merged PDF download with stats displayed"
    - "Drag-and-drop reorder works in the Merge file list using renamed merge-specific handlers"
  artifacts:
    - path: "index.html"
      provides: "Merge PDFs nav button with data-tool='merge-pdf'"
      contains: "data-tool=\"merge-pdf\""
    - path: "index.html"
      provides: "div#merge-pdf.tool-section HTML section"
      contains: "id=\"merge-pdf\""
    - path: "index.html"
      provides: "Merge PDF JS block with independent state and handlers"
      contains: "// ========== MERGE PDFs SECTION =========="
  key_links:
    - from: "nav button data-tool='merge-pdf'"
      to: "div#merge-pdf.tool-section"
      via: "existing nav handler reads dataset.tool and getElementById"
      pattern: "data-tool=\"merge-pdf\""
    - from: "submitBtnMerge click listener"
      to: "mergePDFs() function"
      via: "direct addEventListener"
      pattern: "submitBtnMerge.*addEventListener.*click.*mergePDFs"
    - from: "window.removeMergeFile"
      to: "removeMergeFile() function"
      via: "global exposure for inline onclick"
      pattern: "window\\.removeMergeFile"
---

<objective>
Replace the combined PDF Tools nav tab and HTML section with an independent Merge PDFs nav tab, HTML section, and JS block. This is the first half of splitting the unified pdf-tools section into two independent tools.

Purpose: Merge PDFs needs its own nav tab with independent state so it doesn't share DOM elements or state variables with PDF to Images. This eliminates the confusing radio-toggle UX.

Output: Working Merge PDFs tab with all functionality intact — upload, file list, drag reorder, merge, download, stats.
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-split-merge-pdfs-and-pdf-to-images-into-separate-nav-tabs/03-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs from the existing codebase -->

Nav button pattern (index.html line 895):
```html
<button class="nav-link active" data-tool="pdf-tools">PDF Tools</button>
```

Section pattern (index.html line 904):
```html
<div id="pdf-tools" class="tool-section active">
```

Nav handler (index.html lines 1186-1195) — auto-handles any data-tool button:
```javascript
navLinks.forEach(link => {
    link.addEventListener('click', () => {
        const toolId = link.dataset.tool;
        navLinks.forEach(l => l.classList.remove('active'));
        link.classList.add('active');
        toolSections.forEach(section => section.classList.remove('active'));
        document.getElementById(toolId).classList.add('active');
    });
});
```

Shared utility functions (already generic, take IDs as arguments — no changes needed):
- `showStatus(message, type, statusId)` — line 1352
- `hideStatus(statusId)` — line 1359
- `updateProgress(percent, barId, fillId)` — line 1389
- `hideProgress(barId, fillId)` — line 1394
- `escapeHtml(text)` — line 1399

Current unified block boundary: lines 1198-2431 (// ========== PDF TOOLS (UNIFIED) ==========)
Next section after unified block: line 2433 (// ========== WATERMARK INSERTER ==========)

Drag-and-drop functions to rename (lines 2207-2260):
- `attachPdfDragListeners()` -> `attachMergeDragListeners()`
- `handlePdfDragStart()` -> `handleMergeDragStart()`
- `handlePdfDragEnd()` -> `handleMergeDragEnd()`
- `handlePdfDragOver()` -> `handleMergeDragOver()`
- `handlePdfDragLeave()` -> `handleMergeDragLeave()`
- `handlePdfDrop()` -> `handleMergeDrop()`

Variable/ref rename map from RESEARCH:
- `selectedPdfFiles` -> `mergeFiles`
- `draggedIndex` -> `mergeDraggedIndex`
- `uploadAreaPdf` -> `uploadAreaMerge`
- `fileInputPdf` -> `fileInputMerge`
- `fileListPdf` -> `fileListMerge`
- `submitBtnPdf` -> `submitBtnMerge`
- `clearBtnPdf` -> `clearBtnMerge`
- `statusPdf` -> `statusMerge` (as string ID)
- `progressBarPdf` -> `progressBarMerge` (as string ID)
- `progressFillPdf` -> `progressFillMerge` (as string ID)
- `reorderHintPdf` -> `reorderHintMerge`
- `statsPdf` -> `statsMerge`
- `renderPdfFileList()` -> `renderMergeFileList()`
- `updatePdfButtons()` -> `updateMergeButtons()`
- `showPdfStats()` -> `showMergeStats()`
- `hidePdfStats()` -> `hideMergeStats()`
- `removePdfFile()` -> `removeMergeFile()`
- `window.removePdfFile` -> `window.removeMergeFile`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace nav buttons and create Merge PDF HTML section</name>
  <files>index.html</files>
  <action>
  In index.html, make these HTML changes:

  1. **Replace the pdf-tools nav button** (line 895) with TWO new buttons:
     ```html
     <button class="nav-link active" data-tool="merge-pdf">Merge PDFs</button>
     <button class="nav-link" data-tool="pdf-to-images">PDF to Images</button>
     ```
     Keep the emojis that are currently on the buttons (use the emoji convention from existing code). The merge-pdf button gets `active` class since it replaces pdf-tools as the default tab.

  2. **Replace the entire `<div id="pdf-tools" class="tool-section active">` section** (lines 904-960) with TWO new sections:

     **Merge PDF section** (gets `active` class — it's the default tab):
     ```html
     <!-- MERGE PDFs SECTION -->
     <div id="merge-pdf" class="tool-section active">
         <h1>Merge PDFs</h1>
         <p class="subtitle">Combine multiple PDF files into one document</p>

         <div class="privacy-badge">
             <strong>100% Private</strong> - All processing happens in your browser. No files are uploaded to any server.
         </div>

         <div class="upload-area" id="uploadAreaMerge">
             <div class="upload-icon">upload icon</div>
             <div class="upload-text">Click to upload or drag and drop</div>
             <div class="upload-hint">Upload 2 or more PDF files (no size limit)</div>
             <input type="file" id="fileInputMerge" accept=".pdf,application/pdf" multiple />
         </div>

         <div class="file-list" id="fileListMerge"></div>

         <div class="reorder-hint" id="reorderHintMerge" style="display: none;">
             <strong>Tip:</strong> Drag and drop files to reorder them. PDFs will be merged in this order.
         </div>

         <div class="actions">
             <button class="clear-btn" id="clearBtnMerge" disabled>Clear All</button>
             <button class="submit-btn" id="submitBtnMerge" disabled>Merge PDFs</button>
         </div>

         <div class="progress-bar" id="progressBarMerge">
             <div class="progress-fill" id="progressFillMerge"></div>
         </div>

         <div class="status" id="statusMerge"></div>
         <div class="stats" id="statsMerge"></div>
     </div>
     ```

     **PDF to Images section** (placeholder — Plan 02 will wire the JS):
     ```html
     <!-- PDF TO IMAGES SECTION -->
     <div id="pdf-to-images" class="tool-section">
         <h1>PDF to Images</h1>
         <p class="subtitle">Convert each PDF page to a downloadable image</p>

         <div class="privacy-badge">
             <strong>100% Private</strong> - All processing happens in your browser. No files are uploaded to any server.
         </div>

         <div class="upload-area" id="uploadAreaImages">
             <div class="upload-icon">upload icon</div>
             <div class="upload-text">Click to upload or drag and drop</div>
             <div class="upload-hint">Upload a single PDF to convert each page to an image</div>
             <input type="file" id="fileInputImages" accept=".pdf,application/pdf" />
         </div>

         <div class="file-list" id="fileListImages"></div>

         <div class="actions">
             <button class="clear-btn" id="clearBtnImages" disabled>Clear</button>
             <button class="submit-btn" id="submitBtnImages" disabled>Convert to Images</button>
         </div>

         <div class="progress-bar" id="progressBarImages">
             <div class="progress-fill" id="progressFillImages"></div>
         </div>

         <div class="status" id="statusImages"></div>

         <div class="image-grid" id="imageGridImages"></div>
     </div>
     ```

     IMPORTANT: Preserve the exact emoji style used by the existing sections (each h1, upload-icon, etc. in the existing code uses emojis — match that style). The `fileInputImages` does NOT have the `multiple` attribute (single file only). The `imageGridPdf` element is renamed to `imageGridImages`. The merge section does NOT have an image-grid element. The images section does NOT have a stats or reorder-hint element.

  3. **Remove the old mode selection radio buttons** — the `<div class="options-group">` block (lines 912-931) is inside the old pdf-tools section and gets removed when that section is replaced.

  CRITICAL: The old `<div id="pdf-tools">` section (lines 903-960) is ENTIRELY REPLACED by the two new sections above. Do not leave any remnant of the old section.
  </action>
  <verify>
    <automated>grep -c "id=\"merge-pdf\"" index.html && grep -c "id=\"pdf-to-images\"" index.html && grep -c "id=\"pdf-tools\"" index.html | grep "^0$" && grep -c "data-tool=\"merge-pdf\"" index.html && grep -c "data-tool=\"pdf-to-images\"" index.html && grep -c "pdfToolMode" index.html</automated>
  </verify>
  <done>
  - `id="merge-pdf"` section exists with `class="tool-section active"`
  - `id="pdf-to-images"` section exists with `class="tool-section"` (no active)
  - `id="pdf-tools"` does NOT exist anywhere in the HTML
  - Both nav buttons exist with correct data-tool attributes
  - `merge-pdf` nav button has `class="nav-link active"`
  - No radio buttons for pdfToolMode remain in the HTML
  - All merge-specific DOM element IDs use the `Merge` suffix (uploadAreaMerge, fileInputMerge, etc.)
  - All images-specific DOM element IDs use the `Images` suffix (uploadAreaImages, fileInputImages, etc.)
  - fileInputImages does NOT have the `multiple` attribute
  - fileInputMerge HAS the `multiple` attribute
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Merge PDF JS block replacing the unified PDF tools block</name>
  <files>index.html</files>
  <action>
  Replace the ENTIRE `// ========== PDF TOOLS (UNIFIED) ==========` JS block (lines 1198-2431) with TWO new blocks. This task creates the MERGE block. Plan 02 Task 1 will create the IMAGES block.

  **IMPORTANT:** The existing unified block includes:
  - DOM refs (lines 1199-1211)
  - State variables (lines 1212-1214)
  - Mode selection listeners (lines 1216-1246)
  - Upload handlers (lines 1248-1305)
  - renderPdfFileList (lines 1307-1337)
  - updatePdfButtons (lines 1339-1349)
  - Shared utilities (showStatus, hideStatus, showPdfStats, hidePdfStats, updateProgress, hideProgress, escapeHtml — lines 1352-1403)
  - attachPdfDragListeners + drag handlers (lines 2207-2260)
  - mergePDFs (lines 2262-2343)
  - convertPdfToImages (lines 2345-2410)
  - downloadImage (lines 2412-2419)
  - handlePdfToolSubmit (lines 2421-2427)
  - Event listeners + global exposure (lines 2429-2431)

  **Replace with this structure:**

  ```
  // ========== SHARED UTILITIES ========== (lines 1352-1403 — KEEP UNCHANGED)
  // showStatus, hideStatus, updateProgress, hideProgress, escapeHtml
  // These are already generic (take IDs as args). Do NOT rename or duplicate them.

  // ========== MERGE PDFs SECTION ==========

  // --- State ---
  let mergeFiles = [];
  let mergeDraggedIndex = null;

  // --- DOM references ---
  const uploadAreaMerge = document.getElementById('uploadAreaMerge');
  const fileInputMerge = document.getElementById('fileInputMerge');
  const fileListMerge = document.getElementById('fileListMerge');
  const submitBtnMerge = document.getElementById('submitBtnMerge');
  const clearBtnMerge = document.getElementById('clearBtnMerge');
  const reorderHintMerge = document.getElementById('reorderHintMerge');
  const statsMerge = document.getElementById('statsMerge');

  // --- Upload handlers ---
  // Port from existing code, replacing:
  //   uploadAreaPdf -> uploadAreaMerge
  //   fileInputPdf -> fileInputMerge
  //   selectedPdfFiles -> mergeFiles
  //   statusPdf -> statusMerge (string ID)
  //   Remove ALL pdfToolMode branches — merge always accepts multiple files
  //   Remove the pdfToolMode === 'convert' single-file logic

  // click handler: uploadAreaMerge.addEventListener('click', () => fileInputMerge.click())
  // dragover handler: same pattern
  // dragleave handler: same pattern
  // drop handler: filter PDFs, mergeFiles = [...mergeFiles, ...files], renderMergeFileList(), updateMergeButtons(), hideStatus('statusMerge')
  // change handler: mergeFiles = [...mergeFiles, ...Array.from(e.target.files)], renderMergeFileList(), updateMergeButtons(), hideStatus('statusMerge')
  // clear handler: mergeFiles = [], fileInputMerge.value = '', renderMergeFileList(), updateMergeButtons(), hideStatus('statusMerge'), hideMergeStats()

  // --- removeMergeFile ---
  function removeMergeFile(index) { mergeFiles.splice(index, 1); renderMergeFileList(); updateMergeButtons(); }

  // --- renderMergeFileList ---
  // Port from renderPdfFileList, replacing:
  //   fileListPdf -> fileListMerge
  //   selectedPdfFiles -> mergeFiles
  //   removePdfFile -> removeMergeFile
  //   attachPdfDragListeners -> attachMergeDragListeners
  //   reorderHintPdf -> reorderHintMerge
  // Remove pdfToolMode check entirely — merge always shows reorder hint when 2+ files

  // --- updateMergeButtons ---
  function updateMergeButtons() { submitBtnMerge.disabled = mergeFiles.length < 2; clearBtnMerge.disabled = mergeFiles.length === 0; }

  // --- showMergeStats / hideMergeStats ---
  // Port from showPdfStats/hidePdfStats, replacing statsPdf -> statsMerge

  // --- Drag-and-drop handlers ---
  // Port all 6 functions from attachPdfDragListeners block, renaming:
  //   attachPdfDragListeners -> attachMergeDragListeners
  //   handlePdfDragStart -> handleMergeDragStart
  //   handlePdfDragEnd -> handleMergeDragEnd
  //   handlePdfDragOver -> handleMergeDragOver
  //   handlePdfDragLeave -> handleMergeDragLeave
  //   handlePdfDrop -> handleMergeDrop
  //   fileListPdf -> fileListMerge
  //   draggedIndex -> mergeDraggedIndex
  //   selectedPdfFiles -> mergeFiles
  //   renderPdfFileList -> renderMergeFileList

  // --- mergePDFs ---
  // Port from existing mergePDFs(), replacing:
  //   selectedPdfFiles -> mergeFiles
  //   submitBtnPdf -> submitBtnMerge
  //   clearBtnPdf -> clearBtnMerge
  //   statusPdf -> 'statusMerge'
  //   progressBarPdf -> 'progressBarMerge'
  //   progressFillPdf -> 'progressFillMerge'
  //   showPdfStats -> showMergeStats

  // --- Event listeners + globals ---
  submitBtnMerge.addEventListener('click', mergePDFs);
  window.removeMergeFile = removeMergeFile;
  ```

  **CRITICAL DETAILS:**
  - Do NOT include `pdfToolMode`, `updatePdfToolMode()`, mode radio listeners, or `handlePdfToolSubmit()` — these are DEAD CODE and must not appear
  - Do NOT include `convertPdfToImages`, `downloadImage`, or any images-related code — that goes in Plan 02
  - Do NOT duplicate shared utilities (showStatus, hideStatus, updateProgress, hideProgress, escapeHtml) — they stay as-is in their current position
  - Keep `uploadHintPdf` reference REMOVED — the merge section has a static upload hint, not a dynamic one
  - The `submitBtnMerge` click listener calls `mergePDFs` DIRECTLY (not through a dispatcher)
  - `renderMergeFileList` must use `onclick="removeMergeFile(${index})"` in the template literal (matching `window.removeMergeFile` global)

  **ALSO IN THIS TASK:** Create a PLACEHOLDER comment for the PDF to Images JS block:
  ```javascript
  // ========== PDF TO IMAGES SECTION ==========
  // TODO: Plan 02 will create this block
  ```

  This ensures the code is syntactically valid after this task — the PDF to Images section HTML exists but its JS is not yet wired, so buttons will be non-functional until Plan 02.
  </action>
  <verify>
    <automated>grep -c "MERGE PDFs SECTION" index.html && grep -c "mergeFiles" index.html && grep -c "removeMergeFile" index.html && grep -c "attachMergeDragListeners" index.html && grep -c "pdfToolMode" index.html | grep "^0$" && grep -c "handlePdfToolSubmit" index.html | grep "^0$" && grep -c "updatePdfToolMode" index.html | grep "^0$"</automated>
  </verify>
  <done>
  - `// ========== MERGE PDFs SECTION ==========` header exists
  - `mergeFiles` state array is declared (not `selectedPdfFiles`)
  - `mergeDraggedIndex` declared (not `draggedIndex`)
  - All merge DOM refs use Merge suffix (uploadAreaMerge, fileInputMerge, etc.)
  - `renderMergeFileList()` function exists and calls `attachMergeDragListeners()`
  - `updateMergeButtons()` function exists
  - `showMergeStats()` and `hideMergeStats()` functions exist
  - All 6 drag handler functions renamed to Merge prefix
  - `mergePDFs()` uses mergeFiles, submitBtnMerge, 'statusMerge', 'progressBarMerge', 'progressFillMerge'
  - `submitBtnMerge.addEventListener('click', mergePDFs)` — direct binding, no dispatcher
  - `window.removeMergeFile = removeMergeFile` global exposure
  - NO `pdfToolMode` variable exists anywhere
  - NO `updatePdfToolMode` function exists
  - NO `handlePdfToolSubmit` function exists
  - NO `selectedPdfFiles` variable exists (in merge block)
  - Shared utilities (showStatus, hideStatus, etc.) remain unchanged
  - Placeholder for PDF to Images block exists
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. Open index.html in browser — Merge PDFs tab should be the default active tab
2. Click Merge PDFs tab — section visible with upload area
3. Upload 2+ PDFs — file list shows with drag handles
4. Drag to reorder — order changes correctly
5. Click Merge PDFs — merged PDF downloads, stats shown
6. Click PDF to Images tab — section visible but buttons non-functional (JS not wired yet — Plan 02)
7. No JavaScript console errors on page load
8. `pdfToolMode` search returns 0 hits in the file
</verification>

<success_criteria>
- Merge PDFs is a fully independent nav tab with working upload, reorder, merge, and download
- PDF to Images HTML section exists but JS is not yet wired (placeholder only)
- No vestiges of the old unified pdf-tools section remain
- No shared state between merge and future images sections
- Page loads without JS errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-split-merge-pdfs-and-pdf-to-images-into-separate-nav-tabs/03-01-SUMMARY.md`
</output>
